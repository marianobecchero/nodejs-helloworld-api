pipeline {
    agent any

    tools {nodejs "nodejs"}
    
    stages {
        /*stage("Builasds") {
            steps {
                sh 'npm install'
            }
        }*/
        stage("Test") {
            steps {
                sh 'npm -v'
            }   
        }
    }
}
