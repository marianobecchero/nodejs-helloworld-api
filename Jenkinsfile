pipeline {
    agent any

    tools {nodejs "nodejs"}
    
    stages {
        /*stage("Builasd") {
            steps {
                sh 'npm install'
            }
        }*/
        stage("Test") {
            steps {
                sh 'npm -v'
            }   
        }
    }
}
